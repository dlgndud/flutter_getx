import 'package:flutter/material.dart';
import 'package:get/get.dart';

class GetPutDuumy extends StatelessWidget {
  const GetPutDuumy({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: null,
      ),
    );
  }
}
